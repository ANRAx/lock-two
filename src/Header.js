import React from 'react';

const Header = () => {
    return(
        <header className="bg-light-purple fixed w-100">
            <h1>Find the correct numbers for lock 2!</h1>
        </header>
    );
}

export default Header;